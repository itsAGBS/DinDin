package com.example.sistema_financeiro

import io.flutter.embedding.android.FlutterFragmentActivity

// O local_auth exige que a Activity seja uma FlutterFragmentActivity
// (em vez de FlutterActivity) para conseguir exibir o prompt nativo de
// biometria do Android.
class MainActivity : FlutterFragmentActivity()
